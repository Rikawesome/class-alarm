{
  "module": "app-runtime",
  "role": "composition-root",
  "evolution_policy": "locked",
  "locked_reason": "The composition root controls which evolvable implementations receive protected capabilities and remains human-reviewed in Version 1.",
  "owns": [
    "app/**"
  ],
  "exposes": {
    "genericExtensionHost": "registry-authorized extension discovery, state collection, and action dispatch",
    "GET /api/bootstrap": "read-only course and runtime state",
    "GET /api/events": "alarm notification event stream",
    "POST /api/alarms/test": "fire a non-scheduled demonstration notification",
    "POST /api/courses": "create a validated course and reschedule alarms",
    "POST /api/courses/import": "create a batch of validated courses and reschedule alarms",
    "POST /api/courses/:id/risk": "toggle evolvable risk presentation state",
    "DELETE /api/courses/:id": "delete a course through protected accessors and reschedule alarms"
  },
  "depends_on": [
    "core-data",
    "alarm-engine",
    "personal-data",
    "risk-flag"
  ],
  "invariants": [
    "evolvable modules receive no database implementation",
    "evolvable modules cannot import the protected extension loader",
    "runtime extensions load only from enabled protected registry descriptors",
    "runtime extension capabilities are the exact registry-authorized and manifest-requested set",
    "the application runs without the evolution server",
    "scheduled alarm timing remains owned by alarm-engine"
  ],
  "file_policies": {
    "module.json": "human-review",
    "*.js": "locked",
    "tests/**": "locked"
  },
  "test_policy": {
    "owner": "app-runtime",
    "existing_tests": "locked",
    "new_tests": "human-review"
  },
  "version": "1.3.0",
  "last_updated": "2026-08-09"
}
